# Agent Notes for Fabric Client Source

This source root owns Fabric client adapter code.

- Fabric client lifecycle hooks, payload receivers, ModMenu integration, client mixin declarations, and Fabric-only render hook wiring belong here.
- Shared UI state and screen code may move to common-client only after Fabric and ModMenu imports are removed.
